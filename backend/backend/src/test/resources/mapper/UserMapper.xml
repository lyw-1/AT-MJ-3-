<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mold.digitalization.dao.UserMapper">

    <!-- 澧炲姞鐧诲綍澶辫触娆℃暟 -->
    <update id="incrementLoginFailedCount">
        UPDATE `user` SET login_failed_count = login_failed_count + 1 WHERE id = #{userId}
    </update>

    <!-- 閲嶇疆鐧诲綍澶辫触娆℃暟 -->
    <update id="resetLoginFailedCount">
        UPDATE `user` SET login_failed_count = 0 WHERE id = #{userId}
    </update>

    <!-- 閿佸畾鐢ㄦ埛 -->
    <update id="lockUser">
        UPDATE `user` SET 
            locked_until = #{lockedUntil}, 
            lock_reason = #{lockReason},
            status = 0 
        WHERE id = #{userId}
    </update>
    
    <!-- 瑙ｉ攣鐢ㄦ埛 -->
    <update id="unlockUser">
        UPDATE `user` SET 
            locked_until = NULL, 
            lock_reason = NULL,
            login_failed_count = 0, 
            status = 1 
        WHERE id = #{userId}
    </update>

    <!-- 鏇存柊鐢ㄦ埛鏈€鍚庣櫥褰曚俊鎭?-->
    <update id="updateLastLoginInfo">
        UPDATE `user` SET 
            last_login_ip = #{lastLoginIp}, 
            last_login_time = #{lastLoginTime} 
        WHERE id = #{userId}
    </update>

    <!-- 鏍规嵁鐢ㄦ埛鍚嶆煡璇㈢敤鎴蜂俊鎭?-->
    <select id="selectByUsername" resultType="com.mold.digitalization.entity.User">
        SELECT * FROM `user` WHERE username = #{username} AND deleted = 0
    </select>
</mapper>