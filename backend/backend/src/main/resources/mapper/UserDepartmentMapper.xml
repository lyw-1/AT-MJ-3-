<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mold.digitalization.mapper.UserDepartmentMapper">

  <select id="selectDepartmentNameByUserId" resultType="string">
    SELECT d.name FROM user_department ud
    INNER JOIN department d ON ud.department_id = d.id
    WHERE ud.user_id = #{userId}
    LIMIT 1
  </select>

  <update id="upsertUserDepartment">
    INSERT INTO user_department(user_id, department_id)
    VALUES(#{userId}, #{departmentId})
    ON DUPLICATE KEY UPDATE department_id = VALUES(department_id)
  </update>

</mapper>
