## 更新计划

1. **设计新的主页图标**：创建与Element Plus House图标一致的SVG设计
2. **更新未激活状态图标**：修改 `home.svg` 使用灰色描边和填充
3. **更新激活状态图标**：修改 `home-active.svg` 使用主色调描边和填充
4. **确保图标尺寸一致**：保持现有图标尺寸（81x81）以确保布局一致性
5. **测试图标显示**：确保图标在小程序中正确显示

## 实现细节

- 新图标设计：带有屋顶的房子形状，符合Element Plus House图标的设计风格
- 未激活状态：使用 `#6B7280` 灰色
- 激活状态：使用 `#6150BF` 主色调
- SVG格式：保持与现有图标相同的尺寸和 viewBox 设置

## 预期效果

小程序主页图标将与WEB端Element Plus House图标完全一致，实现跨端视觉统一性。